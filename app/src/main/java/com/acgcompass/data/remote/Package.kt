package com.acgcompass.data.remote

// Package skeleton. Parent of per-source API clients (bangumi/anilist/jikan/mal/vndb) and the
// AI subsystem (ai). Each source: API service + DTO + mapper. Tasks 10, 23, 31-34 (RC.01).
