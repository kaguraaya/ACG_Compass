package com.acgcompass.domain.repository

// Repository interface contracts (领域层, 纯 Kotlin, 无实现). Task 7.2.
// - WorkRepository, BacklogRepository, RatingRepository, SnapshotRepository,
//   TasteRepository, AiRepository.
// - Supporting types: BacklogFilter, BacklogSort, AddResult, BulkOp, DrawCriteria, DrawResult.
// CredentialStore lives in com.acgcompass.data.credential (data layer); not redefined here.
